# environment.yml
name: field
channels:
  - defaults
dependencies:
  - python
  - pip
  # general scientific python code
  - numpy
  - scipy
  - pandas
  - matplotlib
  - seaborn
  # Code maintenance & testing
  - pytest
  - coverage
  - pylint
  # Jupyter related code
  - ipykernel
  - pip:
    - specparam
    - neurodsp