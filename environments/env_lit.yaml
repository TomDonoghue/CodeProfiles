# environment.yml
name: literature
channels:
  - defaults
dependencies:
  - python
  - pip
  # general scientific python code
  - numpy
  - matplotlib
  - seaborn
  - scipy
  # internet requests
  - requests
  - lxml
  - beautifulsoup4
  # Code maintenance & testing
  - pytest
  - coverage
  - pylint
  # Jupyter related code
  - ipykernel
  - pip:
    - wordcloud
    - lisc